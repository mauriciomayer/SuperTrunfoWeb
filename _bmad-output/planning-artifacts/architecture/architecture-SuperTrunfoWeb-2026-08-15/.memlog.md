---
scope: Super Trunfo Web - full system
purpose: build substrate only, no special audience, hobby project
altitude: initiative (whole system, greenfield)
updated: 2026-08-15T02:10
---

- (decision) Caminho Rapido escolhido, mas usuario quer ver as sugestoes de decisoes de peso (paradigma/stack) antes de eu rascunhar o resto. Espinha e o unico entregavel, sem proposito/publico especial.
- (version) Pesquisado na web (2026-08-15): Colyseus 0.17.10 (abr/2026), ativo, rumo a 1.0, framework de jogo autoritativo com salas/rooms, sync de estado, matchmaking, reconexao automatica nativa. Socket.IO 4.8.3 (dez/2025), estavel, generico (nao especifico de jogos). Vite+React+TS e o starter padrao atual para frontend (Vite 5, React 18, TS 5.4 strict).
- (decision) Stack decidido: backend Node.js+TypeScript+Colyseus 0.17.x (framework de jogo autoritativo); frontend React+Vite+TypeScript. Usuario pediu pastas separadas /frontend e /backend (nao misturadas) - monorepo com dois pacotes.
- (version) Pesquisado na web (2026-08-15): Node.js 24 LTS (suporte ate abr/2028) e a LTS atual recomendada para projeto novo; Vite 8.2.x (marco/2026, requer Node 20.19+/22.12+, compativel com Node 24); React 19.2.x (estavel, jul/2026).
- (decision) Paradigma: Servidor Autoritativo (Authoritative Server) - estado canonico da Partida vive so no Colyseus Room (backend); cliente e so view+input, nunca decide regra. Uma Room Colyseus = ciclo de vida completo de uma Partida (espera->jogo->fim), maquina de estados interna, nao Rooms separados por fase.
- (decision) Anti-cheat (RNF-3): visibilidade de carta filtrada por cliente via Colyseus StateView/schema filtering - cada cliente so recebe a propria mao completa + topo visivel dos oponentes quando aplicavel, nunca o Monte completo alheio. Nao confiar em esconder so na UI (falha ja quase cometida na fase de UX).
- (decision) IA roda in-process dentro do Room, chamada sincronamente pela maquina de estados quando e a vez de um Jogador-IA - nao e um client de rede separado.
- (decision) AD resolve FR-7 (PRD 8.3): com 3 jogadores, 32 cartas -> 10 por jogador + 2 descartadas (fora do jogo). [ADOPTED nesta arquitetura].
- (decision) AD resolve FR-12 (PRD 8.4): atributo inverso modelado como campo 'inverso: boolean' em config estatica de atributos, nao hardcoded por nome. Dado atual do CSV: so Aceleracao 0-100km/h e inverso; os demais 6 atributos sao diretos.
- (assumption) AD resolve FR-17 (PRD 8.5), SUPOSICAO nao confirmada com usuario: multiplas cartas letra A na mesma rodada desempatam por ordem de turno (oponente mais proximo do jogador que acionou Super Trunfo, sentido da ordem de assentos, vence).
- (decision) AD ratifica FR-23/PRD 8.6 (proposta ja feita na UX): reconexao devolve controle ao humano so no inicio da proxima Rodada, nunca no meio de uma em andamento. [ADOPTED via UX].
- (decision) Fronteira: /frontend e /backend como pacotes separados no mesmo repo (monorepo simples), comunicando so via protocolo Colyseus/WebSocket - nunca import direto de codigo entre eles. [ADOPTED - pedido explicito do usuario]. Pacote /shared para tipos e deferido (nao obrigatorio em escala hobby).
- (decision) Deployment: processo Node unico hospeda o Colyseus Room server; build estatico do frontend servido separadamente (Vercel/Netlify/Cloudflare Pages) ou pelo mesmo processo Node via arquivos estaticos - hospedagem do backend precisa de processo long-running com WebSocket (Railway/Render/Fly.io ou equivalente), nao serverless. Dimensionado para SM-C1 (poucas partidas simultaneas entre conhecidos) - sem load balancer, sem multi-regiao.
- (event) lint_spine.py: 5 achados baixos, todos falsos-positivos (placeholders {planning_artifacts} sao o alias padrao de path usado em todo o projeto; {grupo}{letra} e documentacao literal do formato de ID, nao um token de template nao preenchido). Nenhuma correcao necessaria.
- (event) Reviewer Gate disparado: rubric walker + 2 finalize_reviewers configurados (verificacao de versoes via web, ataque adversarial de incompatibilidade) em paralelo.
- (change) Revisor de versoes encontrou erro real: 'colyseus.js' nao existe na serie 0.17 (parou em 0.16.22), foi renomeado para @colyseus/sdk (~0.17.42). Corrigido na tabela Stack e no source tree. TypeScript: nota adicionada sobre TS 7.0 (compilador nativo, GA jul/2026) existir mas nao ter compatibilidade verificada com Colyseus - mantido 5.7+ como escolha segura.
- (change) Rubric walker: achado critico - envelope de implantacao estava totalmente ausente da espinha (so no memlog, esquecimento real). Corrigido: novo AD-11 (processo Node long-running para backend, build estatico para frontend, sem staging). Corrigido AD-2 para especificar joinById(roomId) via link, nunca joinOrCreate generico (evita desconhecido entrar na sala errada). AD-8 'ordem de assentos' agora definida como join order do Colyseus. AD-9 ganhou mecanismo de reconexao (par nome+roomId, sem login) e modo espectador ate a proxima rodada. AD-7 e AD-9 alinhados com AD-8 em tom de suposicao nao confirmada. PRD 8.1 (qual carta e o Super Trunfo) adicionado ao Deferred.
- (event) Ataque adversarial: 10 furos encontrados (3 altos, 3 medio-altos, 2 medios, 2 baixos). Todos genuinos - contrato de mensagens ausente, forma de schema de Rodada/Funil ausente, ambiguidade carta-inteira-vs-valor na revelacao, semantica de 3 entradas diferentes em AguardandoSelecao colapsada, definicao de Rodada da AD-9 contradiz FR-19 (cada ciclo de Funil e sua propria Rodada), timing de chamada da IA nao regulado (reabre a race que AD-5 existe pra prevenir), mecanismo de identidade de reconexao nao fixado, distincao estado-canonico-vs-filtrado ausente, regra de Jogador Inicial nao fixada, formula de sobra do AD-6 especifica demais. Aplicando correcoes.
- (change) Todos os 10 furos do ataque adversarial fechados: contrato de mensagens fechado (jogarCarta unifica atributo normal e Super Trunfo, servidor decide pela flag); AD-3 agora distingue estado canonico (game/ e IA) de view filtrada (rede) e fixa que a revelacao manda a carta inteira; AD-4 exige aplicacao atomica da decisao da IA, atraso so visual no cliente; AD-5 fixa as 3 semanticas de entrada de AguardandoSelecao e a regra do Jogador Inicial (host); schema de Rodada/Funil como campos nested documentados; AD-6 virou formula geral; AD-9 redefine 'proxima rodada' como a cadeia inteira de desempate (resolvendo contradicao com FR-19) e fixa mecanismo (allowReconnection + token, janela 60s, nunca so por nome). AD-11 (implantacao) reposicionado para dentro do bloco de Invariants & Rules junto dos demais ADs.
- (event) spine finalized
- (decision) Update durante criacao de epicos/historias: usuario confirmou todas as suposicoes pendentes. AD-5 (Jogador Inicial = host), AD-7 (atributos inversos), AD-8 (desempate letra A por ordem de assentos) - todas viraram CONFIRMADO PELO USUARIO. AD-9 simplificada drasticamente: usuario decidiu que NAO havera reconexao nesta versao - IA assume o assento permanentemente pelo resto da partida, mesmo se o jogador original voltar. Removido mecanismo allowReconnection/token/janela 60s/modo espectador da regra (viraram Deferred, revisitar se o projeto evoluir). AD-4 Prevents ajustado (nao menciona mais handoff de reconexao). AD-11 (plataforma de deploy) permanece deliberadamente em aberto - usuario nao decidiu ainda.
