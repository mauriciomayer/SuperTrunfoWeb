# Deferred Work

<!-- Append-only. Do not modify or delete existing entries. -->

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffolding-do-projeto.md`
  summary: Backend não faz shutdown gracioso (SIGTERM/SIGINT) para descartar Rooms de forma limpa.
  evidence: Fora de escopo da Story 1.1 (sem infra de produção nesta história), mas vai importar quando a Story 1.5 (Publicar o Jogo) fizer deploy/restart real.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffolding-do-projeto.md`
  summary: A porta do backend (2567) está duplicada como literal em `backend/src/index.ts`, `playwright.config.ts` e `frontend/src/client/colyseusClient.ts`, sem fonte única.
  evidence: Não dá pra compartilhar uma constante entre frontend/backend sem violar AD-10 (sem import cruzado); precisa de solução via variável de ambiente numa história futura (provavelmente 1.5).

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffolding-do-projeto.md`
  summary: `backend/` não tem script de lint (frontend tem `oxlint`) e nenhum dos dois está plugado num pipeline de CI.
  evidence: Proporcional pra escopo hobby por enquanto, mas vale decidir se o projeto crescer.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-1-scaffolding-do-projeto.md`
  summary: `PartidaRoom.onLeave` não usa nem testa o parâmetro `consented` (saída limpa vs. desconexão abrupta).
  evidence: Vai importar no Épico 3, quando a saída abrupta precisa disparar o takeover de IA (AD-9) -- distinguir de uma saída intencional do jogador.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-criar-sala.md`
  summary: `PartidaRoom.onLeave` não remove o `Jogador` correspondente de `EstadoPartida.jogadores` -- quem sai fica como entrada fantasma pra sempre.
  evidence: Atualização da lista quando alguém entra/sai é explicitamente escopo da Story 1.4 (Boundaries do spec 1.2), mas a limpeza de estado em si é pré-requisito pra 1.4 construir em cima corretamente.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-criar-sala.md`
  summary: `SalaDeEspera` não trata `room.onLeave`/`room.onError` -- se a conexão cair enquanto o host espera, a tela continua mostrando o último estado conhecido sem nenhum aviso.
  evidence: Conecta com a decisão maior de "sem reconexão" (AD-9); melhor decidir junto com o trabalho de takeover de IA do Épico 3 do que isoladamente aqui.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-criar-sala.md`
  summary: Não existe forma de cancelar/sair da Sala de Espera de volta pra Criar Sala se o host errou a configuração.
  evidence: Escopo novo, não exigido por nenhum FR/AC desta história; vale considerar quando a Story 1.4 construir a Sala de Espera completa.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-2-criar-sala.md`
  summary: Recarregar a página na Sala de Espera perde a referência ao `room` (sem `roomId` persistido/reconexão), abandonando a sala recém-criada.
  evidence: Mesmo território da decisão deliberada de "sem reconexão" (AD-9) pro projeto inteiro; só vale revisitar se AD-9 for revisitada.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-entrar-na-sala.md`
  summary: `entrarSala()`/`criarSala()` não têm timeout -- se a chamada de rede travar, o botão fica preso em "Entrando…"/"Criando…" pra sempre, sem forma de tentar de novo.
  evidence: Afeta as duas telas de entrada (Criar Sala e Entrar na Sala) igualmente; vale uma solução única em vez de duas correções ad-hoc.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-entrar-na-sala.md`
  summary: O campo de nome não tem limite de tamanho no cliente, e o servidor (`PartidaRoom.onJoin`) também não limita -- agora afeta host e convidado.
  evidence: Gap herdado da Story 1.2, agora presente em duas telas; melhor resolver uma vez só, de forma consolidada, do que corrigir cada tela isoladamente.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-3-entrar-na-sala.md`
  summary: Sem distinção visual entre dois Jogadores com o mesmo nome de exibição (ex: dois "Rafael" na mesma Partida) -- `sessionId` distingue por baixo dos panos, mas a lista não mostra isso.
  evidence: Plausível numa Partida em família; nenhum FR/UX exige tratamento disso hoje, mas vale reconsiderar se aparecer na prática.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-sala-de-espera.md`
  summary: `.btn-primario` está duplicado (mesmo bloco CSS) em `CriarSala.css`, `EntrarSala.css` e agora `SalaDeEspera.css`, cada um escopado por tela pra evitar colisão de cascata.
  evidence: Consolidar num componente/classe compartilhada tocaria as três telas de uma vez, fora do escopo desta história (Boundaries não permite mexer em `CriarSala`/`EntrarSala`); vale uma passada única quando fizer sentido tocar as três juntas.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-4-sala-de-espera.md`
  summary: Quando o handler real de `iniciarPartida` for escrito no backend (Épico 2), ele precisa validar de novo que quem mandou é o host -- hoje o gate é só client-side (o botão some da UI, mas `room.send("iniciarPartida")` não é bloqueado por ninguém).
  evidence: Inofensivo agora porque não existe handler nenhum ainda; a regra "só do host" já está documentada na tabela de mensagens da AD-1, então isso é mais um lembrete do que um gap novo.

- source_spec: `_bmad-output/implementation-artifacts/spec-1-5-publicar-o-jogo.md`
  summary: `DEPLOY.md` não avisa que, no plano gratuito do Render, a instância "dormir" por inatividade durante uma Partida em andamento derruba as conexões WebSocket abertas e o estado em memória da Sala/Partida -- só o atraso de acordar no acesso inicial está documentado, não a perda de estado no meio do jogo.
  evidence: Baixa prioridade -- pra uma sessão familiar de uso concentrado, a instância dificilmente dorme no meio de uma Partida ativa; vale só se o plano gratuito continuar sendo usado depois de uso real.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-baralho-distribuicao-e-minha-carta.md`
  summary: `PartidaRoom.onLeave` agora tem uma Mão de verdade (Monte distribuído) pra destruir se alguém sai depois de `iniciarPartida` -- some do `state.jogadores` sem reatribuir host nem limpar `jogadorDaVez` se for quem saiu era o Jogador da vez.
  evidence: Território do takeover de IA por desconexão (AD-9, Épico 3) -- implementar isso agora seria antecipar escopo do Épico 3 dentro do Épico 2; melhor resolver os dois juntos quando o Épico 3 chegar.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-baralho-distribuicao-e-minha-carta.md`
  summary: O padrão "forçar re-render via `useState(0)` + `room.onStateChange`" está duplicado em `SalaDeEspera.tsx`, `MesaDeJogo.tsx` e `App.tsx` -- cada tela reinstala o próprio listener em vez de compartilhar um hook.
  evidence: Consolidar num hook (`useRoomState(room)`) tocaria `SalaDeEspera.tsx`, fora do escopo desta história (Boundaries proíbem mexer nela aqui); vale uma passada única quando fizer sentido tocar as três telas juntas.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-comparacao-vencedor-e-proxima-rodada.md`
  summary: Um Jogador (principalmente o vencedor) se desconectando durante os 2,5s de `Revelando` é uma instância nova e mais severa do gap de desconexão em partida já rastreado (perda de Cartas + `jogadorDaVez` travado, não só "entrada fantasma") -- esta história só blindou contra o crash/corrupção, sem implementar o comportamento correto de jogo pra esse caso.
  evidence: Território do takeover de IA por desconexão (AD-9, Épico 3), mesma razão dos itens já registrados nas Stories 2.1/1.2 -- decidir o comportamento "certo" (o que acontece com as Cartas/vez de um vencedor desconectado) é decisão de design de jogo, não só código, melhor resolvida junto com o resto do Épico 3.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-comparacao-vencedor-e-proxima-rodada.md`
  summary: O teste E2E de resolução de Rodada escolhe um Atributo com baixa probabilidade de empate (~0,6% no baralho real) em vez de forçar um resultado determinístico -- um empate real faria esse teste falhar por timeout confuso em vez de um sinal claro.
  evidence: Forçar determinismo exigiria um hook de teste só pra isso no código de produção do servidor (os testes de integração conseguem mockar `embaralhar` porque rodam a Room em processo; o E2E sobe um servidor real separado) -- risco residual aceito, baixa prioridade pra um projeto hobby.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-carta-super-trunfo.md`
  summary: Nenhum teste E2E (Playwright) cobre o fluxo de Super Trunfo -- a Verification do spec pedia estender `e2e/mesa-de-jogo.spec.ts` forçando a `2A` no topo de alguém, mas isso exigiria um hook de teste dedicado no servidor real (mesmo gap já registrado pra Story 2.3: os testes de integração conseguem mockar `embaralhar` via `vi.mock` porque rodam a Room em processo; o E2E sobe um servidor Colyseus real separado via `npm run dev`, sem esse hook). Sem forçar a Super Trunfo pro topo, esperar ela surgir organicamente numa Partida de 32 Cartas embaralhadas tornaria o teste lento e flaky.
  evidence: Cobertura unitária (`superTrunfo.test.ts`, sem-oposição/uma-Carta-A/múltiplas-Cartas-A-circular-com-wraparound) e de integração de Room (`PartidaRoom.integration.test.ts`, fluxo completo com `embaralharOverride`, incluindo visibilidade via StateView de cliente real) cobrem toda a Matrix e a máquina de estados -- risco residual aceito, mesma classe de gap já documentado pra Story 2.3.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-4-carta-super-trunfo.md`
  summary: Vagas de IA (`Jogador.isIA`) têm `sessionId` sempre `""` (nenhum `Client` de rede associado). Se uma IA vencer qualquer Rodada -- fluxo normal de Atributo (Story 2.2/2.3) ou Super Trunfo (Story 2.4), o defeito é o mesmo raiz e pré-existe esta história -- `rodadaAtual.jogadorDaVez` vira `""`, e nenhum `client.sessionId` de verdade jamais bate com isso (`aoReceberJogarCarta` rejeita silenciosamente pra sempre). A Partida trava permanentemente, sem nenhuma Carta jogável por ninguém.
  evidence: Território do takeover/comportamento de IA (AD-9, Épico 3) -- não existe hoje nenhuma lógica de "vez da IA" (auto-jogar, passar a vez), então mesmo corrigindo a colisão de `sessionId` entre IAs (ver achado do Story 2.4 sobre múltiplas IAs recebendo o destaque `destacada` simultaneamente, corrigido via índice em vez de `sessionId`), o jogo travaria do mesmo jeito quando a vez chegasse numa IA -- resolver isso de verdade é decisão de design de jogo (o que uma IA "joga"?), melhor decidida junto com o resto do Épico 3. Achado pela revisão de edge-case da Story 2.4; risco real e já alcançável em jogo normal com IA desde a Story 2.2/2.3 (não é exclusivo do Super Trunfo), não só uma preocupação hipotética de Épico 3.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-funil-desempate.md`
  summary: Nenhum teste E2E (Playwright) cobre o fluxo de Funil/desempate -- a Verification do spec já aceita esse gap explicitamente ("cenário de Funil forçado NÃO estendido, mesmo gap de determinismo já aceito nas Stories 2.3/2.4"). Forçar um empate real exigiria o mesmo hook de teste dedicado no servidor real que as Stories 2.3/2.4 já não têm (o E2E sobe um servidor Colyseus real via `npm run dev`, sem forma de mockar `embaralhar` como os testes de integração de Room fazem via `vi.mock`); esperar um empate surgir organicamente (~0,6% de chance por Rodada, mesma estimativa da Story 2.3) tornaria o teste lento e flaky.
  evidence: Cobertura de integração de Room (`PartidaRoom.integration.test.ts`, Matrix inteira -- empate simples, nova Rodada sem empate após o Funil, empates consecutivos -- via `embaralharOverride`, incluindo verificação client-decodificada de que `cartasPresas` nunca vaza StateView) cobre a máquina de estados inteira -- risco residual aceito, mesma classe de gap já documentada pras Stories 2.3/2.4.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-funil-desempate.md`
  summary: Se o Jogador que abriu a Rodada empatada (`rodadaAtual.jogadorDaVez`) desconectar durante a pausa de "Revelando" e os Jogadores restantes empatarem entre si nessa mesma Rodada, `resolverRodada` preserva `jogadorDaVez` sem revalidar que essa sessão ainda existe em `state.jogadores` -- ao contrário do caminho vencedor (que já revalida `vencedor` antes de mover Cartas, achado da Story 2.3). `jogadorDaVez` fica apontando pra uma sessão fantasma, travando a Partida pra sempre (nenhum Client real bate com esse sessionId).
  evidence: Território do takeover/desconexão de IA (AD-9, Épico 3), mesma classe de gap já documentada nas Stories 2.1/2.3/2.4 -- antes desta história, QUALQUER empate já travava a Partida incondicionalmente (estado parava em "Funil" pra sempre), então esse cenário especifico de desconexão não conseguia causar dano adicional; agora que empates resolvem e o jogo continua, esse gatilho estreito (abridor da Rodada desconecta durante a pausa E os sobreviventes empatam entre si) passa a ser alcançável de verdade. Achado pela revisão de edge-case da Story 2.5; risco residual aceito, decisão de comportamento "certo" pra sessão fantasma é design de jogo do Épico 3, não só código.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-funil-desempate.md`
  summary: `EXPERIENCE.md` descreve o estado "Empate → Funil" como Cartas "visualmente movidas para o componente Funil" -- a implementação não tem nenhuma animação (as Cartas reveladas simplesmente somem da área de revelação e a tray `Funil.tsx` aparece com a contagem nova, sem transição visual nenhuma ligando "essas Cartas especificas" ao Funil).
  evidence: A spec desta história (aprovada) só pediu a tray com contagem, sem animação -- mesma proporcionalidade de outras simplificações já feitas neste projeto hobby (ex: sem faixa de cabeçalho na Carta, Story 2.1). Vale reconsiderar se/quando o projeto investir em polish visual geral (Cartas "voando" pro Monte do vencedor, já mencionado em `epic-2-context.md` mas também não implementado em nenhuma história do Épico 2 até agora).

- source_spec: `_bmad-output/implementation-artifacts/spec-2-6-fim-de-jogo-e-eliminacao.md`
  summary: Nenhum teste E2E (Playwright) cobre o fluxo de Fim de Partida -- a Verification do spec já aceita esse gap explicitamente ("cenário de Fim de Partida forçado NÃO estendido, mesmo gap de determinismo já aceito nas Stories 2.3/2.4/2.5"). Jogar até alguém reunir o Baralho inteiro organicamente levaria dezenas de Rodadas (lento e flaky); forçar isso exigiria o mesmo hook de teste dedicado no servidor real que as Stories 2.3/2.4/2.5 já não têm (o E2E sobe um servidor Colyseus real via `npm run dev`, sem forma de mockar `embaralhar`/truncar Montes como os testes de integração de Room fazem).
  evidence: Cobertura de integração de Room (`PartidaRoom.integration.test.ts`, Matrix inteira -- eliminação simples, Fim de Partida por coleta, Fim de Partida por atrito, vez pulando o Jogador recém-eliminado, caso degenerado sem crash -- via `embaralharOverride` + truncamento direto de `monte` em `room.state`) e de componente (`FimDePartida.test.tsx`/`MesaDeJogo.test.tsx`, Chip "Eliminado" e Banner de Vitória) cobrem a máquina de estados e a UI inteiras -- risco residual aceito, mesma classe de gap já documentada pras Stories 2.3/2.4/2.5.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-6-fim-de-jogo-e-eliminacao.md`
  summary: O caso totalmente degenerado de `ativos.length === 0` (um empate elimina simultaneamente TODOS os Jogadores que jogaram, ninguém sobra ativo na Partida inteira) não tem nenhum comportamento de jogo "correto" implementado -- `resolverRodada` apenas loga um `warn` e aborta a resolução inteira ANTES de mover qualquer Carta, deixando `estado` congelado em "Revelando"/"SuperTrunfoAcionado" pra sempre (sem nenhum Client conseguir mandar `jogarCarta` dali em diante, já que não é mais a vez de ninguém em `AguardandoSelecao`).
  evidence: Boundaries "Never" do spec desta história marca esse caso explicitamente como decisão de design de jogo futura, não desta história ("Resolver esse empate/vitória-nenhuma de verdade é decisão de design de jogo futura, não desta história"). Coberto por teste de integração de Room (não crasha, não muda `estado`/Cartas, loga `warn`) -- risco residual aceito, comportamento "correto" fica em aberto pra quando o projeto decidir como tratar esse desfecho (ex: empate declarado, revanche automática, etc.).

- source_spec: `_bmad-output/implementation-artifacts/spec-2-6-fim-de-jogo-e-eliminacao.md`
  summary: `EXPERIENCE.md` ("Banner de Vitória") e o mockup aprovado (`key-fim-partida.html`) descrevem confete, um emoji de troféu, e a linha do vencedor destacada em negrito na lista de Jogadores -- nada disso foi implementado em `FimDePartida.tsx`/`.css`; a linha do vencedor renderiza com o mesmo estilo de qualquer Jogador eliminado.
  evidence: A spec desta história (aprovada) só pediu o Banner de Resultado (texto) + lista + botão "Jogar Novamente", sem especificar confete/troféu/negrito -- mesma proporcionalidade de simplificações visuais já aceitas no projeto (ex: sem animação de Cartas "voando" pro Funil, Story 2.5). Achado da revisão blind-hunter; vale reconsiderar junto com qualquer investimento futuro em polish visual geral do Épico 2.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-6-fim-de-jogo-e-eliminacao.md`
  summary: `PartidaRoom.resolverRodada` (branch de empate) acha `jogadorDaVezAtual` comparando `sessionId` (`state.jogadores.find(j => j.sessionId === rodadaAtual.jogadorDaVez)`) -- mesma fragilidade já documentada da Story 2.4 (assentos de IA compartilham `sessionId === ""`, `find`/`findIndex` por `sessionId` pode resolver o assento errado com 2+ IAs). `game/turno.ts` (`proximoJogadorAtivo`) tem a mesma fragilidade internamente.
  evidence: Não é alcançável de verdade hoje: `rodadaAtual.jogadorDaVez` só chega a `""` (sessionId de IA) se uma IA já tiver vencido uma Rodada anterior -- cenário que já trava a Partida permanentemente por um motivo anterior e mais básico (nenhum Client real com sessionId "" consegue mandar `jogarCarta`, achado já registrado nas Stories 2.4/2.5). Corrigir isso aqui isoladamente (comparar por índice em vez de sessionId) não desbloquearia nada sozinho -- é território de "IA jogando de fato" do Épico 3. Achado pela revisão de edge-case da Story 2.6; registrado por completude, mesma classe dos achados já documentados.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-2-selecao-de-atributo-e-revelacao.md`
  summary: O teste "Matrix: atributo ausente (jogarCarta({})) -- rejeitado, nada muda" em `PartidaRoom.integration.test.ts` (helper `criarPartidaEmAguardandoSelecao`) não força a ordem do Baralho -- se o topo real (embaralhado de verdade) do host calhar de ser a Super Trunfo (`2A`, ~1/32 de chance por execução), `jogarCarta({})` deixa de ser rejeitado (Story 2.4 tornou `atributo` opcional pra essa Carta) e o teste falha esperando `AguardandoSelecao` mas recebendo `SuperTrunfoAcionado`.
  evidence: Achado ao rodar a suite de integração durante a verificação independente da Story 2.6 (falha real, não o flake de rede `MatchMakeError: fetch failed` já documentado à parte) -- reproduzido uma vez, confirmado transiente/raro numa nova execução limpa (38/38). Pré-existente desde que a Story 2.4 introduziu a exceção da Super Trunfo; nunca antes registrado. Correção trivial (forçar uma Carta não-Super-Trunfo no topo do host via `embaralharOverride`, mesmo padrão já usado em outros testes desta suite) mas fora do escopo da Story 2.6, que não tocou este teste.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-ia-preenche-e-joga.md`
  summary: RESOLVIDO por esta história -- as entradas anteriores que descreviam a fragilidade de "vagas de IA compartilham `sessionId === ""`" (registradas nas Stories 2.4, 2.5 e 2.6, ex.: `jogadorDaVezAtual`/`vencedor` resolvidos por `sessionId` podendo achar a IA errada) deixam de se aplicar a partir daqui -- a Story 3.1 passou a atribuir um `sessionId` sintético único e estável (`"ia-N"`) pra cada assento de IA na criação (tanto as declaradas em `onCreate` quanto as preenchidas pela rede de segurança em `aoReceberIniciarPartida`), eliminando a ambiguidade na raiz sem precisar tocar `comparacao.ts`/`superTrunfo.ts`/`turno.ts`/frontend.
  evidence: A revisão independente da Story 3.1 (blind-hunter + edge-case-hunter + verification-gap, todos convergindo) encontrou que a ambiguidade não era só uma questão de "qual IA fica com o crédito" -- também causava um crash real (`superTrunfoJogadoPor = remetente.sessionId` colidindo com o sentinel `""` de "sem Super Trunfo nesta Rodada" quando o próprio remetente é uma IA). Corrigido na raiz atribuindo identidade única; as entradas antigas ficam como registro histórico do achado original (arquivo append-only), não removidas, mas não descrevem mais o comportamento atual do código.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-ia-preenche-e-joga.md`
  summary: Se o único Jogador humano (o host) sair da Sala de Espera antes de clicar "Iniciar" numa sala com o resto das vagas já declaradas como IA (ex: `totalJogadores: 4, totalIA: 3`), a sala fica travada em `AguardandoJogadores` pra sempre -- não existe reatribuição de host nem um fallback de início automático, mesmo com todas as outras vagas já jogáveis.
  evidence: Gap de reatribução de host já registrado desde a Story 1.2/2.1 (território de desconexão, AD-9/Épico 3) -- a Story 3.1 não o introduz, mas torna salas "quase todas IA" uma configuração explicitamente incentivada pela feature, tornando esse caminho mais alcançável na prática do que antes. Achado da revisão blind-hunter da Story 3.1.

- source_spec: `_bmad-output/implementation-artifacts/spec-3-1-ia-preenche-e-joga.md`
  summary: Se todos os Jogadores humanos saírem no meio de uma Partida em andamento enquanto 2+ IAs continuam ativas, `onLeave`/`autoDispose` padrão do Colyseus pode derrubar a Room enquanto ainda há um `this.clock.setTimeout` de resolução de Rodada agendado (disparado pela última jogada de IA) -- comportamento não testado nem documentado explicitamente.
  evidence: Território de desconexão/Épico 3 (Story 3.2) -- a Story 3.1 é o que torna "só IA jogando, zero humano presente" um estado de jogo rotineiro pela primeira vez, mas resolver o ciclo de vida da Room nesse cenário é decisão que pertence à Story 3.2, não a esta. Achado da revisão blind-hunter da Story 3.1. NÃO resolvido pela Story 3.2 -- Boundaries "Never" do spec 3.2 marca esse caso (ciclo de vida da Room quando TODOS os humanos saem) explicitamente fora de escopo dela também ("sem 'demais' humanos pra proteger, não é problema desta história"), então este item continua em aberto.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-1-baralho-distribuicao-e-minha-carta.md`
  summary: RESOLVIDO pela Story 3.2 -- a entrada acima ("PartidaRoom.onLeave agora tem uma Mão de verdade... some do state.jogadores sem reatribuir host nem limpar jogadorDaVez") descrevia `onLeave` removendo incondicionalmente qualquer Jogador, mesmo numa Partida em andamento. A partir da Story 3.2, numa Partida em andamento (qualquer `estado` além de `AguardandoJogadores`) o `Jogador` nunca é mais removido -- vira `isIA = true` no lugar, preservando Monte/posição/nome, e dispara a jogada automática (Story 3.1) se era a vez dele.
  evidence: `PartidaRoom.onLeave` (backend/src/rooms/PartidaRoom.ts) bifurcado por `state.estado`; Matrix inteira coberta por `PartidaRoom.integration.test.ts` (describe "Continuidade por Desconexao (Story 3.2)"). A entrada antiga fica como registro histórico do achado original (arquivo append-only), não removida, mas não descreve mais o comportamento atual do código.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-3-comparacao-vencedor-e-proxima-rodada.md`
  summary: RESOLVIDO pela Story 3.2 -- a entrada acima ("Um Jogador -- principalmente o vencedor -- se desconectando durante os 2,5s de Revelando... esta história só blindou contra o crash/corrupção, sem implementar o comportamento correto de jogo pra esse caso") descrevia um gap explicitamente adiado pro Épico 3. Como `onLeave` numa Partida em andamento nunca mais remove o `Jogador` (vira IA no lugar), `resolverRodada` sempre encontra o vencedor -- mesmo que a sessão real dele já tenha caído durante a pausa -- e credita a vitória normalmente, com o Monte real preservado.
  evidence: Teste de integração atualizado (`PartidaRoom.integration.test.ts`, describe "resolverRodada (Story 2.3)", teste renomeado de "Boundaries defensivo: o Jogador que teria vencido desconecta durante Revelando -- resolverRodada não crasha..." pra "Story 3.2: o Jogador que teria vencido desconecta durante Revelando -- vira IA, resolverRodada credita a vitória dele normalmente") -- o teste antigo testava a premissa velha (`onLeave` removendo) e passou a falhar (assentos ficando 2 em vez do 1 esperado) assim que a Story 3.2 mudou o comportamento; atualizado pra provar o caminho feliz novo em vez do abort defensivo antigo. Os `if (!vencedor) return` defensivos em `resolverRodada` continuam no código (nunca fazem mal, e ainda protegem contra qualquer estado inesperado futuro), mas essa classe específica de desconexão-durante-a-pausa deixa de ser alcançável por este caminho.

- source_spec: `_bmad-output/implementation-artifacts/spec-2-5-funil-desempate.md`
  summary: RESOLVIDO pela Story 3.2 -- a entrada acima ("Se o Jogador que abriu a Rodada empatada desconectar durante a pausa de Revelando... jogadorDaVez fica apontando pra uma sessão fantasma, travando a Partida pra sempre") descrevia outro gap adiado pro Épico 3. Como `onLeave` numa Partida em andamento nunca mais remove o `Jogador`, `jogadorDaVezAtual` (`resolverRodada`, branch de empate) sempre encontra uma sessão válida em `state.jogadores` -- na pior das hipóteses, agora controlada por IA.
  evidence: Mesma correção de `PartidaRoom.onLeave` da entrada da Story 2.3 acima resolve os dois gaps na raiz (a causa era idêntica: remoção incondicional de `state.jogadores` numa Partida em andamento). Não coberto por um teste de integração dedicado a este cenário específico (empate + desconexão do abridor) -- risco residual mínimo aceito, já que a Matrix de `onLeave` da Story 3.2 (`PartidaRoom.integration.test.ts`, describe "Continuidade por Desconexao") cobre a causa raiz (o `Jogador` nunca mais some de `state.jogadores`) de forma genérica o suficiente pra também proteger este caminho, mesmo sem uma combinação de teste dedicada a "empate + desconexão do abridor durante a pausa" especificamente.

- source_spec: `_bmad-output/implementation-artifacts/spec-4-1-faq-de-regras.md`
  summary: O conteúdo estático de `frontend/src/screens/FAQ.tsx` (32 Cartas, 7 Atributos, 1 deles inverso, a regra de 2 Cartas descartadas em Partidas de 3 Jogadores) duplica em prosa fatos que o backend já possui como fonte da verdade (`atributos.ts`, `baralho.ts`/`TOTAL_CARTAS_ESPERADO`, `distribuir()`) -- uma mudança futura nesses valores no backend não quebra nenhum build nem teste do frontend, só deixa a FAQ desatualizada em silêncio.
  evidence: Achado da revisão blind-hunter da Story 4.1. Sem solução simples dentro do escopo desta história: AD-10 proíbe qualquer import de código entre `frontend/`/`backend/`, então não há como a FAQ ler esses valores "ao vivo" sem violar essa decisão de arquitetura já fechada -- resolver isso exigiria uma decisão de arquitetura nova (ex: um endpoint/JSON compartilhado, ou gerar a FAQ a partir do mesmo CSV), fora de escopo de uma tela de conteúdo estático.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-resultado-da-rodada-sempre-visivel.md`
  summary: `ultimoResultado` (backend, `PartidaRoom.ts`) só é limpo no branch de empate (`vencedorNome`/`atributo` zerados) -- em Rodadas consecutivas sem empate, ele é apenas sobrescrito quando a PRÓXIMA Rodada resolve, nunca zerado entre elas. Antes da Story 5.1 isso era inofensivo (o Chip ficava invisível abaixo da dobra na maior parte do tempo, então ninguém notava o dado "velho" ali). Depois da Story 5.1 (Chip vira overlay sempre visível), esse mesmo dado velho fica fixado no topo da tela durante TODA a Rodada seguinte, inclusive durante a revelação dela -- mostrando o resultado da Rodada ANTERIOR como se fosse atual, o que é mais confuso do que o bug original.
  evidence: Achado da revisão blind-hunter da Story 5.1, confirmado lendo `PartidaRoom.ts` diretamente (`ultimoResultado.vencedorNome = ""` só no branch de empate, linha ~715; sobrescrito sem limpeza prévia no branch sem-empate, linha ~904). Fora do escopo da Story 5.1 -- o Boundaries dela proíbe explicitamente qualquer mudança em `resolverRodada`/`PartidaRoom.ts` (história definida como "puramente CSS/posicionamento"). Conserto correto provavelmente exige limpar `ultimoResultado` no início da próxima Rodada (não só no empate) -- candidato a história futura dedicada.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-1-resultado-da-rodada-sempre-visivel.md`
  summary: `.chip-resultado--overlay` (novo, `position: fixed`) não considera `env(safe-area-inset-top)` (pode renderizar parcialmente sob o notch/status bar em celulares com tela recortada) nem o comportamento de `position: fixed` com a barra de ferramentas dinâmica do Safari em iOS (onde elementos fixos são medidos contra o viewport de layout, não o visual, um problema conhecido da plataforma).
  evidence: Achados de edge-case-hunter/blind-hunter da Story 5.1. Risco residual aceito como baixa prioridade -- nenhum outro overlay do projeto trata isso hoje, a suite E2E só cobre Chromium (sem alvo Safari), e é um projeto hobby pra uso familiar, não public-facing em escala que justifique o esforço agora.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-4-fotos-reais-dos-carros.md`
  summary: `backend/src/game/baralho.ts` não valida que a coluna `Imagem` (nem nenhuma outra coluna do CSV) exista de fato no cabeçalho antes de ler `registro["Imagem"]` -- se a coluna for renomeada/removida por engano numa edição futura do CSV, `carta.imagem` vira `undefined` em silêncio (viola o tipo `string` do schema) em vez de falhar cedo e alto, ao contrário da validação de contagem de colunas/32-Cartas/1-Super-Trunfo que a mesma função já faz.
  evidence: Achado independente de blind-hunter e edge-case-hunter da Story 5.4. Não é um gap introduzido por esta história -- o mesmo padrão (nenhuma validação de presença por coluna individual) já existia pra `Pais`, `Grupo`, etc. antes dela; corrigir só pra `Imagem` isoladamente criaria inconsistência entre campos. Vale uma passada única cobrindo todas as colunas nomeadas, se/quando o CSV for editado por outra pessoa além do Mauricio.

- source_spec: `_bmad-output/implementation-artifacts/spec-5-4-fotos-reais-dos-carros.md`
  summary: O mapa `FOTOS_POR_ARQUIVO` (`Carta.tsx`, construído via `import.meta.glob` sobre `assets/carros/*`) não detecta/avisa se dois arquivos futuros compartilharem o mesmo nome-base (ex: variação de maiúscula/minúscula) -- o `Object.fromEntries` simplesmente deixa o último sobrescrever o anterior em silêncio.
  evidence: Achado de edge-case-hunter da Story 5.4. Não alcançável hoje -- os 32 arquivos atuais já foram verificados como únicos na implementação; risco é só pra uma futura Carta #33+ sem checagem de duplicidade na hora de nomear o arquivo. Correção trivial (checar se a chave já existe antes de sobrescrever, `console.warn` se sim) mas sem valor demonstrável no estado atual dos dados.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-2-mesa-de-jogo-em-tela-cheia-no-desktop.md`
  summary: No layout desktop novo (`.mesa-de-jogo__oponentes`, `MesaDeJogo.css`), quando o scroll interno defensivo (`overflow-y: auto`) chega a engatar no pior caso alcançável (3-4 Jogadores, vários oponentes revelados ao mesmo tempo numa viewport baixa), a coluna fixa de 220px não reserva espaço pra barra de rolagem (`scrollbar-gutter: stable` ausente) -- pode cortar/desalinhar levemente a borda direita do card na coluna. Também: numa tela muito larga (1920px+), com `.app-shell--mesa-de-jogo` soltando o `max-width` por completo, a coluna de oponentes (grade fixa de 220px, `justify-content: center`) fica centralizada numa área `1fr` bem mais larga que o necessário, deixando vãos grandes ao redor -- não quebra nada, só não é esteticamente ideal.
  evidence: Achados de edge-case-hunter da Story 6.2. Ambos cosméticos, só se manifestam em cenários raros/compostos (o primeiro exige o pior caso de 3-4 Jogadores revelados simultaneamente E o scroll interno já ativo; o segundo só em monitores ultrawide/4K, incomuns pro uso familiar deste projeto hobby) -- risco residual aceito, baixa prioridade.

- source_spec: `_bmad-output/implementation-artifacts/spec-6-3-chip-de-resultado-desaparece-automaticamente.md`
  summary: O `useEffect` novo do Chip de Resultado (`MesaDeJogo.tsx`) rastreia a Rodada anterior via um `useRef` (`estadoAnteriorRef`) mutado incondicionalmente no fim do efeito, sem a mutação ser desfeita no cleanup. Sob `<StrictMode>` (`main.tsx` já envolve o app inteiro nisso), o dev double-invoke de um efeito de MONTAGEM (setup -> cleanup -> setup, sem re-render no meio) faz a 2a chamada de setup encontrar o ref já preenchido pela 1a -- se `MesaDeJogo` algum dia montar com `ultimoResultado.vencedorNome` JÁ preenchido (hoje isso nunca acontece -- `App.tsx` só monta `MesaDeJogo` quando `estado` sai de `AguardandoJogadores`, momento em que o backend acabou de zerar `ultimoResultado`), o Chip apareceria mas o timer de ~3s de auto-esconder NUNCA seria agendado (só o clearTimeout da 1a chamada roda; a 2a chamada não reagenda por já não se ver como "primeira renderização"), ficando preso na tela até a próxima Rodada realmente resolver.
  evidence: Achado independente e coincidente de blind-hunter e edge-case-hunter da Story 6.3 (mesmo mecanismo, rastreado linha a linha nas duas revisões). Inalcançável hoje -- `MesaDeJogo` nunca remonta em jogo (confirmado em `App.tsx`: só sai de "mesaDeJogo" ao chegar em "FimDePartida"; não existe reconexão/retomada de sessão no projeto, AD-9), e o `<StrictMode>` double-invoke é removido em build de produção. Só passaria a importar se uma história futura adicionar reconexão ou entrada de espectador (montando `MesaDeJogo` no meio de uma Rodada já resolvida). Correção não é trivial -- uma tentativa óbvia (reverter `estadoAnteriorRef.current` no cleanup) quebra o rastreamento real de transições entre Rodadas de verdade (o cleanup de um efeito reverteria pro valor anterior À TRANSIÇÃO que o motivou, não só à invocação sintética do StrictMode); exige redesenho cuidadoso se/quando virar alcançável.
  update: A Story 7.1 adicionou reconexão real (`allowReconnection`) à Sala de Espera -- mas `MesaDeJogo` continua nunca remontando em jogo, e a reconexão da Story 7.1 é só pra ANTES da Partida começar (`AguardandoJogadores`), nunca durante -- este item continua inalcançável, sem mudança de status.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-reconexao-curta-na-sala-de-espera.md`
  summary: Nenhum teste cobre 2+ desconexões abruptas simultâneas na Sala de Espera (2 Jogadores diferentes no meio da janela de reconexão ao mesmo tempo) -- a propriedade central do `Set` (`sessoesReconectando`, `PartidaRoom.ts`) de continuar bloqueando `iniciarPartida` até TODAS as entradas esvaziarem, não só a primeira, fica sem prova direta.
  evidence: Achado de blind-hunter da 2a rodada de revisão da Story 7.1. Risco real baixo -- a correção depende só da semântica padrão de `Set<string>.add`/`.delete` (cada `sessionId` é uma chave independente), sem lógica adicional específica da Room que pudesse divergir por Jogador; não é o cenário do incidente original (que envolveu só 1 conexão caindo).

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-reconexao-curta-na-sala-de-espera.md`
  summary: O teste "janela de reconexão expira sem reconexão" (`PartidaRoom.integration.test.ts`) espera o tempo real de `JANELA_RECONEXAO_SALA_DE_ESPERA_S` (30s) com só 5s de margem (`timeout: 35000`) antes de considerar o teste travado -- risco de flakiness se a suite rodar num ambiente genuinamente mais lento/carregado.
  evidence: Achado de blind-hunter da 2a rodada de revisão da Story 7.1. Risco aceito por ora -- o projeto não tem pipeline de CI (já registrado desde a Story 1.1), então a suite só roda localmente, onde essa margem se mostrou suficiente em múltiplas execuções durante a própria implementação/revisão desta história.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-1-reconexao-curta-na-sala-de-espera.md`
  summary: Existe uma janela estreita entre o framework resolver a `Deferred` de `allowReconnection` (permitindo `onReconnect` rodar) e o `.finally()` de `onDrop` efetivamente remover o `sessionId` de `sessoesReconectando` -- um `iniciarPartida` chegando exatamente nesse intervalo poderia ser rejeitado pelo guard novo mesmo com a reconexão já tendo sucedido.
  evidence: Achado de blind-hunter da 2a rodada de revisão da Story 7.1. Consequência branda -- o guard já rejeita silenciosamente sem mensagem de erro (mesmo padrão dos outros guards de `aoReceberIniciarPartida`), então o host só precisaria clicar "Iniciar" de novo um instante depois; não trava a Partida nem perde dado nenhum, ao contrário do achado original (assento fantasma) que motivou o guard.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-aviso-de-conexao-perdida-na-sala-de-espera.md`
  summary: Se a própria conexão (`onLeave`/`onError`) cair ANTES do primeiro snapshot de `room.state` chegar (`jogadores` ainda vazio), `souHost` cai no fallback `false` -- um host nessa janela estreita veria a mensagem orientada pra convidado ("Reabra o link de convite...") em vez de "Crie uma nova sala pra continuar.".
  evidence: Achado independente de blind-hunter e edge-case-hunter da Story 7.2. Janela extremamente estreita (o mesmo intervalo entre handshake e primeiro patch de estado já documentado no componente pra outro propósito) -- uma correção de verdade exigiria expandir o escopo aprovado do spec (`SalaDeEspera.tsx`/`.css`/teste, só) pra `App.tsx` também, passando um sinal explícito de "sou host" que não dependa do array `jogadores` sincronizar. Ação orientada ao usuário (recriar a sala/reabrir o link) continua funcionando nos dois casos, só o texto fica trocado nessa janela rara.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-aviso-de-conexao-perdida-na-sala-de-espera.md`
  summary: Se a conexão já tiver caído/dado erro ANTES do `useEffect` de `SalaDeEspera.tsx` assinar `onLeave`/`onError` (corrida de montagem, ainda mais estreita que a anterior), o aviso nunca aparece -- a tela renderiza normalmente sem saber que a conexão já está morta.
  evidence: Achado de edge-case-hunter da Story 7.2. Inalcançável na prática hoje -- `SalaDeEspera` só monta com uma conexão já viva (pós `criarSala`/`entrarSala` resolvidos), e o `useEffect` roda essencialmente no mesmo ciclo do mount; risco residual mínimo aceito.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-aviso-de-conexao-perdida-na-sala-de-espera.md`
  summary: `onError` (client-side) trata QUALQUER erro como definitivo (`conexaoPerdida` nunca volta a `false`) -- se o SDK algum dia disparasse `onError` pra uma condição não-fatal (conexão continua utilizável depois), o aviso ficaria preso na tela pra sempre, mesmo com a sala funcionando.
  evidence: Achado de edge-case-hunter da Story 7.2. Risco baixo -- rastreado no código-fonte do `@colyseus/sdk` (`Room.mjs`) que `onError` só dispara hoje pra fechamento de conexão antes de qualquer join bem-sucedido (não alcançável aqui, `SalaDeEspera` já monta pós-join) ou erro bruto de WebSocket (que no navegador quase sempre é seguido de fechamento real da conexão). Uma correção robusta exigiria também escutar `onReconnect` pra resetar a flag -- fora do escopo aprovado desta história (Boundaries "Never": nunca gerenciar reconexão manualmente).

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-aviso-de-conexao-perdida-na-sala-de-espera.md`
  summary: Se uma futura história adicionar um botão de "sair da sala" (ação intencional, hoje inexistente em `SalaDeEspera.tsx`), ele dispararia `room.onLeave` do mesmo jeito que uma queda abrupta -- mostrando o aviso alarmante "Sua conexão com a sala caiu." pra uma saída deliberada do próprio Jogador.
  evidence: Achado de blind-hunter da Story 7.2. Nenhuma funcionalidade de "sair da sala" existe hoje no projeto -- nota pra quem implementar isso no futuro considerar distinguir saída consentida (`code === CONSENTED`) da queda abrupta antes de mostrar este aviso.

- source_spec: `_bmad-output/implementation-artifacts/spec-7-2-aviso-de-conexao-perdida-na-sala-de-espera.md`
  summary: Enquanto o SDK tenta reconectar sozinho automaticamente (até 15 tentativas, backoff até 5s cada -- ver Story 7.1), a Sala de Espera fica parada sem nenhum indicador visual de que algo está acontecendo, por até dezenas de segundos, antes do aviso de conexão perdida finalmente aparecer (quando os retries esgotam).
  evidence: Achado de blind-hunter da Story 7.2, já antecipado nas Design Notes do próprio spec. Uma melhoria futura possível seria usar o signal `onDrop` (client-side) pra mostrar um estado intermediário "reconectando…" -- deliberadamente fora do escopo desta história (Boundaries "Never"), que só cobre o aviso de falha definitiva.
