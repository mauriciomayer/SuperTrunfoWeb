/**
 * Placeholder de `src/game/` (Story 1.1 -- scaffolding).
 *
 * Este arquivo nao contem regra de jogo real -- so existe para dar a
 * primeira camada da piramide de testes (AD-12) algo puro e trivial pra
 * exercitar. A regra de jogo de verdade (comparacao de atributos, Funil,
 * Super Trunfo, IA) chega no Epico 2 e 3.
 */
export function somar(a: number, b: number): number {
  return a + b;
}
